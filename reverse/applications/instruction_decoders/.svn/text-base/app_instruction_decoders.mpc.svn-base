project(class_disassembler) : install_bin {
    exename = class_disassembler
    after = reverse
    includes += $(top_srcdir)

    libs = reverse
    lit_libs = boost_program_options boost_date_time

    Source_Files {
	class_disassembler.cpp
    }
}
